function loadItems() {
    spoitemsss = JSON.parse(get("spoitems"));


    if (_.size(spoitemsss) >= count) {
        count = _.size(spoitemsss) + 1;
    }

    if (spoitemsss) {
        var organizado = {};
        $.each(spoitemsss, function () {
            var t = this;
            organizado[t.position] = this;
        });
    }
    get("spoitems") && (total = 0, $("#poTable tbody").empty(), spoitems = JSON.parse(get("spoitems")), $.each(organizado, function () {
        var t = this, e = 1 == Settings.item_addition ? t.item_id : t.id;
        spoitems[e] = t;

        var o = t.row.id, s = t.row.cost, i = t.row.qty, a = t.row.code, n = t.row.name.replace(/"/g, "&#034;").replace(/'/g, "&#039;"), d = (new Date).getTime(), r = $('<tr id="' + d + '" class="' + e + '" data-item-id="' + e + '"></tr>');
        tr_html = '<td style="min-width:100px;"><input name="product_id[]" type="hidden" class="rid" value="' + o + '"><span class="sname" id="name_' + d + '">' + n + " (" + a + ")</span></td>",
                tr_html += '<td style="padding:2px;"><input class="form-control input-sm kb-pad text-center rquantity" name="quantity[]" type="text" value="' + i + '" data-id="' + d + '" data-item="' + e + '" id="quantity_' + d + '" onClick="this.select();"></td>',
                tr_html += '<td style="padding:2px; min-width:80px;"><input class="form-control input-sm kb-pad text-center rcost" name="cost[]" type="text" value="' + s + '" data-id="' + d + '" data-item="' + e + '" id="cost_' + d + '" onClick="this.select();"></td>',
                tr_html += '<td style="padding:2px; min-width:80px;"><input class="form-control input-sm kb-pad text-center rmargin" name="margin[]" type="text" value="' + t.row.margen + '" data-id="' + d + '" data-item="' + e + '" id="margin_' + d + '" onClick="this.select();"></td>',
                tr_html += '<td style="padding:2px; min-width:80px;"><input class="form-control input-sm kb-pad text-center rprecio" name="price[]" type="text" precioactual="' + t.row.price + '" value="' + t.row.price + '" data-id="' + d + '" data-item="' + e + '" id="precio_' + d + '" onClick="this.select();"></td>',
                tr_html += '<td class="text-right"><span class="text-right ssubtotal" id="subtotal_' + d + '">' + formatMoney(parseFloat(s) * parseFloat(i)) + "</span></td>",
                tr_html += '<td class="text-center"><i class="fa fa-trash-o tip pointer spodel" id="' + d + '" title="Remove"></i></td>', r.html(tr_html), r.prependTo("#poTable"), total += parseFloat(s) * parseFloat(i)
    }), grand_total = formatMoney(total), $("#gtotal").text(grand_total), $("#add_item").focus())

    $('.rmargin').on('keyup', function () {
        margen = $(this).val();
        cost = $(this).parent().prev().children().val();
        price = $(this).parent().next().children();
        porcentaje = (parseFloat(cost) * parseFloat(margen)) / 100;
        precio = parseFloat(cost) + porcentaje;
        price.val(precio);
    });

    $('.rcost').on('keyup', function () {
        cost = $(this).val();
        margen = $(this).parent().next().children();
        price = $(this).parent().next().next().children();
        porcentaje = (parseFloat(cost) * parseFloat(margen.val())) / 100;
        precio = parseFloat(cost) + porcentaje;
        price.val(precio);
    });


}
function add_order_item(t) {
    var e = 1 == Settings.item_addition ? t.item_id : t.id;
    if (!spoitems[e]) {
        t.position = count;
        count = count + 1;
    }

    return spoitems[e] ? spoitems[e].row.qty = parseFloat(spoitems[e].row.qty) + 1 : spoitems[e] = t, store("spoitems", JSON.stringify(spoitems)), loadItems(), !0
}
$(document).ready(function () {

    loadItems(), $("#date").inputmask("yyyy-mm-dd hh:mm", {placeholder: "yyyy-mm-dd hh:mm"}), $(document).on("click", ".spodel", function () {
        var t = $(this).closest("tr"), e = t.attr("data-item-id");
        if (delete spoitems[e], t.remove(), !spoitems.hasOwnProperty(e))
            return localStorage.setItem("spoitems", JSON.stringify(spoitems)), void loadItems()
    }), $("#add_item").autocomplete({
        source: base_url + "purchases/suggestions",
        minLength: 1,
        autoFocus: !1,
        delay: 200,
        response: function (t, e) {
            $(this).val().length >= 16 && 0 == e.content[0].id ? (bootbox.alert(lang.no_match_found, function () {
                $("#add_item").focus()
            }), $(this).val("")) : 1 == e.content.length && 0 != e.content[0].id ? (e.item = e.content[0], $(this).data("ui-autocomplete")._trigger("select", "autocompleteselect", e), $(this).autocomplete("close")) : 1 == e.content.length && 0 == e.content[0].id && (bootbox.alert(lang.no_match_found, function () {
                $("#add_item").focus()
            }), $(this).val(""))
        },
        select: function (t, e) {
            t.preventDefault(), 0 !== e.item.id ? add_order_item(e.item) && $(this).val("") : bootbox.alert(lang.no_match_found)
        }
    }), $("#add_item").bind("keypress", function (t) {
        13 == t.keyCode && (t.preventDefault(), $(this).autocomplete("search"))
    }), $("#add_item").focus(), $("#reset").click(function (t) {
        bootbox.confirm(lang.r_u_sure, function (t) {
            t && (get("spoitems") && remove("spoitems"), window.location.reload())
        })
    }), $(document).on("change", ".rquantity", function () {

        var t = $(this).closest("tr"), e = parseFloat($(this).val()), o = t.attr("data-item-id");
        spoitems[o].row.qty = e, store("spoitems", JSON.stringify(spoitems)), loadItems()

    }), $(document).on("change", ".rcost", function () {

        price = $(this).parent().next().next().children();
        price.trigger('change');
        var t = $(this).closest("tr"), e = parseFloat($(this).val()), o = t.attr("data-item-id");
        spoitems[o].row.cost = e, store("spoitems", JSON.stringify(spoitems)), loadItems()

    }), $(document).on("change", ".rprecio", function () {
        
        var t = $(this).closest("tr"), e = parseFloat($(this).val()), o = t.attr("data-item-id");
        spoitems[o].row.price = e, store("spoitems", JSON.stringify(spoitems)), loadItems()
        
    }), $(document).on("change", ".rmargin", function () {
        
        price = $(this).parent().next().children();
        console.log(price);
        price.trigger('change');
        var t = $(this).closest("tr"), e = parseFloat($(this).val()), o = t.attr("data-item-id");
        spoitems[o].row.margin = e, store("spoitems", JSON.stringify(spoitems)), loadItems()
    })
});
//# sourceMappingURL=maps/purchases.min.js.map
